// Re-export shim for project convention. The generated types live at
// src/integrations/supabase/types.ts and must not be edited.
export type { Database, Json } from "@/integrations/supabase/types";
